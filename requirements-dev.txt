# =====================================================================
# Pipeline_Integrado — Dependencias de desarrollo (testing, linting)
# =====================================================================
# Instalar después de requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# =====================================================================

-r requirements.txt

# --- Testing ---
pytest==8.2.2
pytest-cov==5.0.0
pytest-xdist==3.6.1
hypothesis==6.108.5

# --- Ejecución de notebooks en tests de integración ---
nbconvert==7.16.4
nbclient==0.10.0

# --- Linting / formateo (opcional, no bloqueante) ---
ruff==0.5.7
